{
  "mcpServers": {
    "lunatalk-card-writer-local": {
      "type": "http",
      "url": "${LUNATALK_CARD_WRITER_MCP_URL}",
      "headers": {
        "Authorization": "Bearer ${LUNATALK_ACCOUNT_TOKEN}"
      }
    }
  }
}
